import React from 'react';

const Character = (props) => {
      const {birthday,category,img,name,nickname,portrayed,occupation,status} = props
      return (
            <div>
                  <h1>character</h1>
            </div>
      );
};

export default Character;